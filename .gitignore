node_modules/
dist/
dist-electron/
.env
.env.local
*.db
*.db-journal
*.db-wal
*.db-shm
.vite/
packages/database/prisma/generated/
.turbo/
.claude/mcp.json
.encryption_key
.api_token
*.log
# Stray output from ad-hoc `npm run … > run_out.txt 2> run_err.txt` debugging.
run_out.txt
run_err.txt
# Local demo-data seeder: hardcodes this machine's DB path and a one-off demo
# narrative, and writes Meeting with a raw 32-column INSERT that breaks on any
# schema change. Run it from the repo root; it is not part of the app.
/seed-demo-meeting.mjs
